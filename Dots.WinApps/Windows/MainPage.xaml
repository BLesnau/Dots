<Page
    x:Class="Dots.WinApps.Windows.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dots.WinApps.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="using:Dots.WinApps.Shared"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=shared:MainPageVm}"
>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Name="LoginStack" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <Button Content="Facebook" HorizontalAlignment="Stretch" Command="{Binding FacebookClick}"></Button>
            <Button Content="Google" HorizontalAlignment="Stretch" Command="{Binding GoogleClick}"></Button>
            <Button Content="Twitter" HorizontalAlignment="Stretch" Command="{Binding TwitterClick}"></Button>
            <Button Content="Microsoft" HorizontalAlignment="Stretch" Command="{Binding MicrosoftClick}"></Button>
        </StackPanel>
        <StackPanel Name="LogoutStack" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <Button Content="Logout" HorizontalAlignment="Stretch" Command="{Binding LogoutClick}"></Button>
        </StackPanel>
    </Grid>
</Page>
